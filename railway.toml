[build]
buildCommand = "npm ci && LOCODE_MODE=web npm run build"

[deploy]
startCommand = "node .output/server/index.mjs"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
